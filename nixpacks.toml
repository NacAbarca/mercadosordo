[phases.setup]
nixPkgs = ["php82", "php82Extensions.pdo", "php82Extensions.pdo_mysql", "php82Extensions.mbstring", "php82Extensions.fileinfo", "php82Extensions.curl", "php82Extensions.gd"]

[phases.build]
cmds = ["mkdir -p public/uploads/products public/uploads/avatars"]

[start]
cmd = "php -S 0.0.0.0:$PORT -t public -c php.ini"

